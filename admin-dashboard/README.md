# BINOVA Admin Dashboard

Frontend for the BINOVA admin dashboard (Vite + React).

Install:

```bash
cd admin-dashboard
npm install
npm run dev
```

API base is read from `VITE_API_URL` in `.env` (defaults to http://localhost:3000).
